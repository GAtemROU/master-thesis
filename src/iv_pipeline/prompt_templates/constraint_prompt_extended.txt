Output exactly one line. No other text.

Format:
INTERVAL: [a, b]

Rules:
- Do NOT compute any new numbers
- Do NOT combine numbers
- Use only numbers already in the problem
- If you compute the exact answer, the output is invalid

Problem:
{question}